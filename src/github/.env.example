# IMPORTANTE: Token do GitHub
# Para operações com GitHub (GraphQL API), você PRECISA usar um token CLÁSSICO (ghp_)
# Os tokens fine-grained (github_pat_) NÃO FUNCIONAM para operações GraphQL do GitHub

# TOKEN CLÁSSICO RECOMENDADO para operações GraphQL (Projects V2)
GITHUB_TOKEN=ghp_...

# Escopos necessários para o token:
# - repo (acesso completo aos repositórios)
# - admin:org ou read:org (acesso aos recursos da organização)
# - project (acesso a projetos)

# Nível de log (debug, info, warn, error)
LOG_LEVEL=debug

# Detalhes de diagnóstico nos logs (true/false)
LOG_DIAGNOSTICS=true

# Desativar completamente os logs (true/false)
# IMPORTANTE: Recomendado 'true' para MCP servers para evitar problemas de formatação JSON
DISABLE_LOGS=true 